migrate:
  - version: 3.6.0
    scripts:
      - "ALTER TABLE %PREFIX%teams ADD team_name VARCHAR(16)"
  - version: 4.0.0
    scripts:
      - "ALTER TABLE %PREFIX%players DROP player_name"
      - "ALTER TABLE %PREFIX%players DROP last_joined"
      - "ALTER TABLE %PREFIX%settings DROP INDEX settings_index"
      - "ALTER TABLE %PREFIX%settings ADD PRIMARY KEY (settings_name)"
  - version: 4.5.0
    scripts:
      - "ALTER TABLE %PREFIX%players ADD fixed_home TEXT"
  - version: 5.0.0
    scripts:
      - "ALTER TABLE %PREFIX%players ADD peaceful_mode BOOL"
